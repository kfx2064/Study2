<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.hdcd.mapper.TestMapper">

    <select id="list" resultType="org.hdcd.domain.Board">
        <![CDATA[
        SELECT  board_no AS boardNo
             , title
             , content
             , writer
             , reg_date AS regDate
        FROM    board
        WHERE   board_no > 0
        ORDER BY    board_no DESC
               , reg_date DESC
        ]]>
    </select>

    <select id="itemList" resultType="org.hdcd.domain.Item">
        <![CDATA[
        SELECT  item_id
             , item_name
             , price
             , description
             , picture_url
        FROM    item
        WHERE   item_id > 0
        ORDER BY    item_id DESC
        ]]>
    </select>

</mapper>